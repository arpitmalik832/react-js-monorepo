/**
 * @file Mock SVG component for testing.
 */
/**
 * Mock SVG React component for testing purposes.
 * @returns {JSX.Element} An empty div element that simulates an SVG component.
 * @example
 * // Using the mock SVG component in tests
 * <ReactComponent />
 */
const ReactComponent = () => <div />;

export default ReactComponent;
